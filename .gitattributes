# Shell scripts must always have LF line endings, regardless of the local
# git config on Windows checkouts - CRLF breaks the shebang inside Linux
# containers (e.g. the Docker build for Railway).
*.sh text eol=lf
